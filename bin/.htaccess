# Non-public directory — deny all direct web access.
# The application reads these files from the filesystem (PHP), never over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
